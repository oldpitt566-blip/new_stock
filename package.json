{
  "name": "stock-price-server",
  "version": "1.0.0",
  "description": "Stock price scraper for Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "echo 'Checking Pip...'; pip --version || pip3 --version; echo 'Installing Requirements...'; pip install -r requirements.txt || pip3 install -r requirements.txt || echo 'Pip failed! Check Build Logs.'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
